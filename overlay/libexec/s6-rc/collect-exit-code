#!/bin/execlineb
foreground {
    elgetpositionals
    emptyenv -P
    $@
}
multisubstitute {
    importas -u EXITCODE ?
    import -i S6_SCAN_DIR
}
foreground {
    redirfd -w 1 ${S6_SCAN_DIR}/.exitcode
    s6-echo $EXITCODE
}
s6-svscanctl -t ${S6_SCAN_DIR}
